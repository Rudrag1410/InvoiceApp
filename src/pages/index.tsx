import Dashboard from "container/dashboard";
import Layout from "components/layout";

const Home = (): JSX.Element => {
  return (
    <Layout>
      <Dashboard />
    </Layout>
  );
};

export default Home;
